class Product:

    def __init__(self, name, stock):
        self.name = name
        self.stock = stock

    def add_stock(self, qty):
        self.stock += qty

    def display(self):
        print(self.name, self.stock)

p = Product("Mouse", 20)
p.add_stock(10)
p.display()
  Output
Mouse 30
