class Armstrong:

    def check(self, n):

        total = 0
        temp = n

        while temp > 0:
            digit = temp % 10
            total += digit ** 3
            temp //= 10

        if total == n:
            print("Armstrong")
        else:
            print("Not Armstrong")

a = Armstrong()

a.check(153)
Output
Armstrong
