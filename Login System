class Login:

    def login(self, user, password):
        if user == "admin" and password == "1234":
            print("Login Successful")
        else:
            print("Invalid Login")

l = Login()
l.login("admin", "1234")
Output
Login Successful
